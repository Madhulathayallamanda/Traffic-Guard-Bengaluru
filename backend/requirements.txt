# Core
flask==3.1.3
pandas>=2.2,<3.1
gdown>=5.2.0

# Reports (CSV is native pandas; PDF needs these two)
reportlab==4.4.10
matplotlib>=3.8

# Optional — only needed if you want to read/write violations.parquet directly.
# If unavailable, app.py automatically falls back to building the dataset
# from data/raw_violations.csv at startup (slower one-time load, same result).
pyarrow==17.0.0
